import React from 'react';

require('../assets/sass/header.scss');

/**
 * Header component
 */
class Header extends React.Component{

    constructor(){
       super();
    }
    render() {
        return(
            <section id="header">
            </section>

        );
    }
}
export default Header;